# Prepare a `company_id__creditor_company_id` lookup table from Asset Impact's `masterdata_debt` data

Prepare a `company_id__creditor_company_id` lookup table from Asset
Impact's `masterdata_debt` data

## Usage

``` r
prepare_company_id__creditor_company_id(masterdata_debt)
```

## Arguments

- masterdata_debt:

  A data frame containing raw production data from Asset Impact's
  masterdata_debt CSV

## Value

A tibble
